{
  "name": "@scheduler/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "electron .",
    "dist": "electron-builder"
  },
  "dependencies": {
    "@scheduler/core": "workspace:*",
    "@scheduler/plugin-mock": "workspace:*",
    "@scheduler/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.2"
  }
}
